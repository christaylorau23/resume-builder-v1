# Railway/Nixpacks: Node 20 + pnpm — skip Puppeteer Chromium download at install time
[phases.setup]
nixPkgs = ["nodejs_20"]

[variables]
PUPPETEER_SKIP_DOWNLOAD = "true"
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
