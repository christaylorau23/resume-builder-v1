Architecture must strictly separate AI content generation from layout enforcement (Perfect Spacing on iOS Safari).
PDF and layout stack must be chosen for 100% ATS-readable searchable text.
Shared Core must be designed so CLI and Web SPA use the same content and layout pipelines.